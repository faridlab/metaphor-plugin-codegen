# {{MODULE_NAME_PASCAL}} Development Configuration
#
# Overrides for local development

server:
  host: 127.0.0.1

database:
  url: postgresql://root:password@localhost:5432/{{MODULE_NAME}}_dev
  max_connections: 5

logging:
  level: debug
  format: pretty

tracing:
  enabled: false  # Disable in development

metrics:
  enabled: false  # Disable in development
