//! Child Entity Template

syntax = "proto3";

package {{MODULE_NAME}}.domain.entity;

import "google/protobuf/timestamp.proto";
import "buf/validate/validate.proto";

// {{PascalCaseEntity}} Child Entity
//
// This entity belongs to the aggregate root and cannot exist independently.
// It maintains a reference to its parent aggregate.
message {{PascalCaseEntity}} {
  // Unique identifier for the entity
  string id = 1 [
    (buf.validate.field).string.uuid = true,
    (buf.validate.field).required = true
  ];

  // Reference to parent aggregate
  string aggregate_id = 2 [
    (buf.validate.field).string.uuid = true,
    (buf.validate.field).required = true
  ];

TIMESTAMP_FIELDS_PLACEHOLDER

  // TODO: Add entity-specific fields here
  // Example:
  // string name = 5 [(buf.validate.field).string.min_len = 1];
  // int32 value = 6 [(buf.validate.field).int32.gte = 0];
}

// Create {{PascalCaseEntity}} Command
message Create{{PascalCaseEntity}}Command {
  string aggregate_id = 1 [(buf.validate.field).string.uuid = true];

  // TODO: Add creation fields here
  // Example:
  // string name = 2 [(buf.validate.field).string.min_len = 1];
  // int32 initial_value = 3 [(buf.validate.field).int32.gte = 0];
}

// Update {{PascalCaseEntity}} Command
message Update{{PascalCaseEntity}}Command {
  string id = 1 [(buf.validate.field).string.uuid = true];
  string aggregate_id = 2 [(buf.validate.field).string.uuid = true];

  // TODO: Add updatable fields here
  // Example:
  // optional string name = 3 [(buf.validate.field).string.min_len = 1];
  // optional int32 value = 4 [(buf.validate.field).int32.gte = 0];
}